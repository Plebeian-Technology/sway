# typed: true

RSpec.describe "Registration", type: :request do
  describe "POST /create" do
    it 'asks a User to confirm their number if they have no already' do
      # post "https://localhost:3000/users/webauthn/registration"

      # expect(response.parsed_body)
    end
  end
end
