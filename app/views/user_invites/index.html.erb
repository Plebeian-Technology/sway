<p style="color: green"><%= notice %></p>

<h1>User invites</h1>

<div id="user_invites">
  <% @user_invites.each do |user_invite| %>
    <%= render user_invite %>
    <p>
      <%= link_to "Show this user invite", user_invite %>
    </p>
  <% end %>
</div>

<%= link_to "New user invite", new_user_invite_path %>
